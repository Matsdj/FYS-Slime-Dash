//collin
/*void spikeSetup() {
  spikes = new Spikes();
}

Spikes spikes;
class Spikes {
  float x, y, z, size, draw;
  int a = 2;

  size = globalscale;

  void draw() {
    rect(x, y, size, size);
  }
}
ArrayList<Spikes> spikes = new ArrayList();
boolean blockCollision(float x, float y, float size) {
  boolean Collision = false;
  for (int spikeNumber = 0; spikeNumber < blocks.size(); spikeNumber++) {
    if (blocks.get(spikeNumber).x < x+size && blocks.get(spikeNumber).x+size > x && blocks.get(spikeNumber).y < y+size && blocks.get(spikeNumber).y+size > y) {
      Collision = true;
    }
  }
  return Collision;
}

for (int i = 0; i <a; i++) {


  Block block = spikes.get(i);
  spikes.draw();
  rect(spikes.x, spikes.y, globalScale, globalScale);
}
}
}
*/
